{
  "name": "2020_advent_of_code",
  "version": "0.0.1",
  "description": "Advent of Code 2020 Solutions",
  "main": "index.js",
  "scripts": {
    "compile": "tsc -p . --incremental",
    "clean:compile": "rm -rf ./dist && tsc -p . --incremental",
    "test": "npm run compile && tape ./dist/**/*.unit.js",
    "test:glob": "npm run compile && tape ",
    "make-it-so": "npm run compile && node dist/index"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ramda": "^0.27.1",
    "tape": "^5.0.1"
  },
  "devDependencies": {
    "@types/ramda": "^0.27.32",
    "@types/tape": "^4.13.0",
    "typescript": "^4.1.2"
  }
}
